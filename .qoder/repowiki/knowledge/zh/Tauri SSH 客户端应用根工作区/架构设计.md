- 采用 Monorepo 架构，通过 `pnpm-workspace.yaml` 管理多包依赖，确保前端（frontend）与后端（backend）模块在统一的工作区上下文中协同开发。
- 利用 `vite.config.ts` 配置 Tauri 专用的开发服务器（端口 1420/1421），实现前端热更新与后端 Rust 进程的隔离通信。
- 通过根目录 `package.json` 定义统一的脚本入口（如 `tauri dev/build`），协调前端资源打包与后端二进制编译的流水线。