- 所有 Store 结构体均实现 `Default` trait 并暴露 `new()` 构造函数，内部通过 `Mutex` 保护可变状态。
- 持久化逻辑统一采用“先写内存集合，再序列化写入磁盘”的模式，并在写入前自动创建父目录。
- 异步方法统一返回 `Result<T, String>`，错误信息直接转换为字符串以便于前端展示。
- 涉及长时间运行的监听任务（如端口转发）均通过 `tauri::async_runtime::spawn` 在后台异步执行，并通过 `Arc<Notify>` 提供取消机制。