- 所有暴露给前端的业务逻辑均封装在 `#[tauri::command]` 函数中，并统一返回 `Result<T, String>` 以适配 IPC 错误处理。
- 核心状态对象（如会话句柄、转发注册表）通过 `Arc<Mutex<T>>` 包装并注入 Tauri State，确保跨线程安全共享。
- 敏感认证信息（密码、私钥口令）严禁明文持久化，统一委托给 `keyring` 库进行操作系统级别的加密存储。