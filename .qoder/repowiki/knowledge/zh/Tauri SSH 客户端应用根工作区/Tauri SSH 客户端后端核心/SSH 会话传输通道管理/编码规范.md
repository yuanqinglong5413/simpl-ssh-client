- 采用 `tokio::select!` 宏实现双向数据流的并发桥接，确保输入输出通道的实时同步与优雅关闭。
- 使用 `tracing` 宏记录关键网络事件（如连接失败、握手异常），并统一使用 `anyhow::Result` 或 `Option` 处理非致命错误。
- 在涉及跨线程共享状态（如 `TerminalBridge`）时，统一使用 `Arc<Mutex<HashMap>>` 或 `Arc<TokioMutex>` 保证线程安全。