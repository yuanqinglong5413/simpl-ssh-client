- 所有远程操作函数均接收 `SessionManager` 或 `SftpSession` 引用，确保在复用的持久连接上执行而非新建 TCP 连接。
- Git 命令输出解析统一采用纯函数（如 `parse_status`），输入原始字符串，输出强类型的 `Serialize` 结构体，便于前端直接消费。
- 传输任务的状态变更与进度更新通过 Tauri 的 `Emitter` 推送自定义事件（`transfer://state` / `transfer://progress`），实现 UI 与后端逻辑解耦。