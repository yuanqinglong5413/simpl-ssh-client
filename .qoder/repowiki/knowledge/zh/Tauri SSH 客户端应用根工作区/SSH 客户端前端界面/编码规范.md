- 使用 `crypto.randomUUID()` 为标签页、分屏面板及连接请求生成唯一标识符。
- 通过 `useRef` 存储 WebSocket 实例、定时器 ID 及重连状态标志，以避免闭包陷阱并确保副作用管理的稳定性。
- 采用受控组件模式管理对话框（如 `ConnectDialog`、`SettingsDialog`）的显隐状态，并通过回调函数处理业务逻辑。
- 在 `TerminalPane` 等涉及底层资源（WebSocket/PTY）的组件中，利用 `ResizeObserver` 和防抖定时器优化尺寸变更事件的处理。